/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export app > firebase.config.js
 * 5. Build > Authentication > getStarted > enable sign method
 * **/ 

/**
 * 1. Create a context provider file
 * 2. create context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 * **/ 

/**
 * HOSTING
 * one time per pc
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * ------------------
 * For Each projects
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (up and down arrow) and use space bar to select
 * 4. existing project
 * 5. select project
 * 6. which project as public directory : dist
 * 7. single page application: yes
 * 8. continuous deployment: no
 **/

/***
 * FOR EVERY TIME DEPLOY
 * 1. npm run build
 * 2. firebase deploy
 * 
 */